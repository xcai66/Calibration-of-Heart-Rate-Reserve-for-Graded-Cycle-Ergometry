# Figure validation report

Validation date: 2026-08-08

## Static preflight

The final Python plotting source passed all 14 strict checks in the Nature Figure static preflight:

- valid Python syntax;
- publication-safe sans-serif fonts and minimum detected text size above 5 pt;
- no rainbow color map;
- editable SVG/PDF text configuration;
- SVG, PDF, TIFF, and PNG export paths;
- 600-dpi TIFF output;
- 183-mm final width;
- no detected row sampling, silent exclusion, demo-data generator, unguarded log transformation, or cross-backend plotting call.

Result: **14 pass, 0 warnings, 0 failures**.

## Visual inspection

Figures 1-3 were regenerated from machine-readable source tables. PNG renders were visually inspected at original resolution. Panel labels, legends, axis labels, confidence intervals, color contrast, and endpoint annotations were visible and internally consistent. Figure 3 was specifically checked after replacement with the four-panel robustness figure.

## Remaining target-journal action

Confirm the selected journal's exact single- or double-column dimensions, color mode, file-size limits, and TIFF compression before upload. No scientific or layout defect is currently blocking delivery.
